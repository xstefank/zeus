if failed host {{ item.hostname }} port {{ if none(item.port) '80' else item.port }}
          protocol {{ if none(item.protocol) 'HTTPS' else item.protocol }} request {{ item.path }} with timeout {{ if none(item.timeout) '6' else item.timeout }} seconds then alert
