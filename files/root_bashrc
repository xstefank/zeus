# .bashrc

# User specific aliases and functions

alias rm='rm -i'
alias cp='cp -i'
alias mv='mv -i'

# Source global definitions
if [ -f /etc/bashrc ]; then
	. /etc/bashrc
fi

readonly JBOSS_SET_OPS_SCRIPTS_HOME=${JBOSS_SET_OPS_SCRIPTS_HOME:-'/opt/jboss-set-ci-scripts/ops/'}

export PATH=${JBOSS_SET_OPS_SCRIPTS_HOME}:${PATH}
